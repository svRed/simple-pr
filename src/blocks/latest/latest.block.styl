.latest {
    overflow: hidden;
    &__block {
        width: 100%;
        height: 262px
    }
    &__title {
        font-size: 1.5rem;
        text-align: center;
        white-space: nowrap;
        text-overflow: ellipsis;
        overflow: hidden;
    }
}